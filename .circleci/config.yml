version: 2.1

workflows:
  build_workflow:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: cimg/node:18.0
    steps:
      - checkout
      - run:
          name: install dependencies
          command: npm install
